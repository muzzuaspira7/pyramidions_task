import 'package:flutter/material.dart';

class AppColors {
  static const bg1 = Color(0xFF4B0082); // main center color
  static const bg2 = Color(0xFF23261D); // darker background

  static const centerGradientStart = Color(
    0xFF63267D,
  ); // top-left / main gradient start
  static const centerGradientEnd = Color(
    0xFF342862,
  ); // bottom-right / main gradient end
  static const gradientMid1 = Color(0xFF2C173E); // intermediate gradient
  static const gradientMid2 = Color(0xFF1E1634); // dark edge gradient

  static const glassTint = Color(0xFF5f4081);
  static const borderColor = Color(0xFF5B357A);
}
